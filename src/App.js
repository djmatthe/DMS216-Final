import React, { Fragment } from 'react';
import OutTopic from "./components/OutTopic"
import { Lobby } from "./pages"

const App = props => {
  return (
    <Fragment>
      <Lobby/>
    </Fragment>
  );
}

export default App;
