import React, { Fragment } from 'react';
import { Lobby } from "./pages"
import "./index.css"

const App = props => {
  return (
    <Fragment>
      <Lobby/>
    </Fragment>
  );
}

export default App;
